[package]
name = "ubiq-host"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Ubiq: the headless host — processes, pseudo-terminals, and the project catalogue"
publish = false

# The host draws nothing. No GPUI crate may appear in this tree, directly or transitively, and
# `just host` is the check that says so.
[dependencies]
ubiq-proto = { path = "../ubiq-proto" }
portable-pty = "0.9"
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
toml = "1"
directories = "5"
chrono = { version = "0.4", features = ["serde"] }
thiserror = "2"
tracing = "0.1"
flume = "0.12"
# Reading version control: blobs from HEAD and from the index, never the network — which is why
# neither ssh nor https is compiled in.
git2 = { version = "0.21", default-features = false }
# The hunks themselves. A diff library lives here and never in the interface.
similar = "2"

[dev-dependencies]
tempfile = "3"
