---
id: backlog
title: Backlog
kind: tech
status: current
summary: Every open question, known gap and deferred item across the project, in one register.
read_when: you are planning the next piece of work, or you hit something unresolved and need somewhere to put it
updated: 2026-09-10
verified: 2026-09-10
review_cycle: monthly
---

# Backlog

One register for everything unresolved. A `TODO` in a document, an `Open questions` heading, or a
"not decided yet" aside all belong here instead — that is what keeps the rest of the library
readable as a statement of what holds.

Each row names the document it affects, so resolving an item tells you what to update. An item that
ships or is dropped **leaves this file**; its outcome lives in git, or in the decision register if
it settled something structural.

Documentation-structure proposals go to `_meta/feedback.md`, not here. The test: does resolving it
change what Ubiq does (here), or where a document lives (there)?

## Gaps — the tree lacks something the documentation describes

| # | Item | Affects |
|---|---|---|
| G101 | The ACP bridge is unpinned. `AcpBridge` is written against `_docs/references/acp-protocol.md` and never run against a live ACP agent: Grok is not installed on the machine and no other ACP-speaking binary was reachable, so `grok agent stdio` is a reported subcommand rather than a captured one, and whether that mode accepts `--model` at all is unknown. The handshake, the `session/update` shapes and the `fs/*` round trip hold on paper only. Capturing one run and writing it up under `crates/agent-manager/_docs/test-runs/` is what closes this | [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G102 | An ACP agent gets no terminal and no elicitation. `AcpBridge` advertises `fs.readTextFile`, `fs.writeTextFile` and `session.configOptions.boolean`, and answers `terminal/*`, `elicitation/*` and every other unknown inbound method with JSON-RPC `-32601` — legal because the capability was never advertised, and necessary because silence deadlocks the agent. Serving `terminal/*` is a deferral rather than a hole: ACP's terminal is a one-shot captured command with a byte cap, not a pseudo-terminal, so it shares nothing with a pane and needs its own implementation | [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G103 | The `fs/*` confinement is lexical. `AcpBridge` normalizes `..` out of a requested path and refuses anything that leaves the session root, which stops a path escape and not a symlink one: a link inside the root pointing outside it is followed, and the agent reads or writes wherever it lands | [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G212 | The ACP delegate heuristic is the fallback path only. The adapters Ubiq launches implement the ACP subagent draft (PR #1992), which `initialize_params` opts into at `clientCapabilities._meta.jetbrains.air`, so a delegate normally arrives as a `subagent_spawned` plus a child session whose envelope `sessionId` attributes every chunk exactly. What remains is the path for an adapter that ignores the capability: ACP v1 itself names no subagent vocabulary — no parent id, no nesting, no delegation `ToolKind` — so `AcpBridge` reads an `Origin` out of `_meta` where an agent stamps one (`parentToolCallId`/`parentToolUseId`, `subagentType`, `model`, flat or under one vendor object) and otherwise infers it: while a `Task`/`Agent` tool call is open the main agent is blocked on it, so every unattributed line is that delegate's. With two delegates open at once every line is attributed to the most recently opened one, since nothing on the wire could separate them | [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G214 | The ACP subagent extension is a draft carried under a vendor namespace. `subagent_spawned` and `subagent_state_update` come from ACP PR #1992, unmerged, and the capability that gates them is advertised as `_meta.jetbrains.air.capabilities: ["nativeSubagentSessions"]` — JetBrains AIR's namespace, not ACP's. Nothing in `AcpBridge` is version-negotiated beyond `version: 1`, so a rename when the draft lands, or a second vendor shipping the same idea under its own key, silently drops every subagent back onto `G212`'s heuristic with no error and no log line. The claude adapter also accepts the standard `clientCapabilities.subagents: {}` form and the codex one does not, so there is no single spelling to migrate to yet | [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G215 | The ACP bridge never echoes the user's own prompt, and one agent in four does it for us. `io/jsonl.rs` maps a user text block back out as a `UserMessageChunk` so the transcript owns what was sent; `AcpBridge` writes `session/prompt` and emits nothing, so a `claude-code-acp`, `copilot` or `opencode` transcript shows only the agent's half. `grok` alone sends a `user_message_chunk` back, which is why the fix cannot be an unconditional echo: that double-renders the one agent that behaves. Either the bridge echoes only until it sees the agent do it for the current turn, or the interface renders the composer's text itself and the two bridges stop disagreeing | [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G100 | Vim mode stops where the proposal's phase 3 does. No macros (`q`, `@`), marks, ex commands, folds, named registers, `ctrl-o` or replace mode. `j` and `k` move by logical line rather than display line, because the component's display map is not reachable; `/` matches a plain substring rather than a regex; `J` with a count joins one line; and the Normal-mode caret is the component's fixed thin one, since its shape is a hard-coded quad and `InputEditorStyle` is not re-exported | [`features/workbench.md`](./features/workbench.md) |
| G92 | Two bridge shapes ask a human, one does not. Claude's launch carries `--permission-prompt-tool stdio`, a `control_request` becomes an `AgentEvent::PermissionRequest` and stays outstanding until the caller answers it; `AcpBridge` parks `session/request_permission` the same way for every ACP harness — Grok, opencode and Copilot included, since both of the latter dropped their one-shot `--dangerously-skip-permissions`/`--allow-all --no-ask-user` launch for `opencode acp`/`copilot --acp`. Only Codex approves itself: its reader auto-accepts every approval RPC the moment it scans it, so an `AnswerPermission` naming it is a documented no-op and an agent in a column edits any file the user can, with no prompt. Confinement is not part of this: a conversation is sandboxed by `isolate_agents` exactly as a pane is — `confined_launch` renders the policy into an argv the bridge spawns with its own pipes — so `HomeMode` reaches a conversation and `P6` has landed on top of it. On Linux that argv cannot be rendered and a conversation fails to start rather than running unconfined (`G90`) | [`tech/decisions.md`](./tech/decisions.md), [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G94 | Live agents sit beside mock ones in the same list. `crates/ubiq-host/src/work/mod.rs` holds both, and the sidebar draws them together, so a screen can show an agent that answers next to eleven that cannot | [`features/workbench.md`](./features/workbench.md) |
| G95 | A one-shot harness's turns are separate processes, so what a harness carries between them is whatever its own `--resume` restores. The session id is durable — `remember_sessions` writes it to the run's `SessionMeta` as it arrives, so a kill survives it — but a harness that reports no session id at all still answers each prompt with no memory of the last, and nothing can be done about that from this side. The conversation is continuous here and only as continuous as the harness allows there | [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G96 | Only Claude reports a context window, so only Claude draws a ring. Codex, opencode and Copilot report tokens with no window, and a ratio with an invented denominator is worse than none | [`features/workbench.md`](./features/workbench.md) |
| G98 | Ubiq's conversation vocabulary is the Agent Client Protocol's v1. A v2 draft reshapes diffs into structured file changes, makes the message id required, and removes the mode methods outright — `_docs/references/acp-protocol.md` records what is coming | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G99 | Raw harness frames reach the log console only when `RUST_LOG` asks for them by name, because they carry prompts and file contents. There is no control in the console for it | [`features/logs.md`](./features/logs.md) |
| G89 | A profile's composition is chosen wholesale or not at all. `StartConversation` carries a profile id and the settings overlay writes profiles through `FsProfileStore`, so a saved harness, account, model and mode reach a run — but the form edits only those four fields. The skills, MCP servers, instructions, isolation and `extends` chain a `Profile` can carry are still a `profile.toml` written by hand, because none of them is on the wire; `G31` and `G78` are the same gap seen from the catalogue's side | [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G90 | An agent confined in a pane is macOS-only: `agent_manager::isolate::confined_launch` renders the policy and execs `sandbox-exec`, which macOS supports and Landlock cannot. isol8's pty seam replaces it and is in the pinned revision, but `PtyChild` fuses the child with the master — `resize` borrows it and `child` borrows it mutably — so a pane cannot resize on one thread while another waits. The master-only handle that unblocks the switch is requested in `refs/isol8-pty-seam-update.md` §8. Nowhere else has an enforcing backend to switch to: Linux confinement is unwired, and isol8's Windows AppContainer builds a token without enforcing the policy's path grants, so a confined run there would be confined in name only. `confined_launch` erroring on every non-macOS target is the honest form of that until one of the two lands | [`tech/agent-manager.md`](./tech/agent-manager.md), [`features/sessions-and-workspaces.md`](./features/sessions-and-workspaces.md) |
| G91 | A confined agent's toolchain reach is four hand-maintained lists. `agent_manager::isolate`'s `DEV_LAYERS` names 15 isol8 layers because isol8 auto-selects only from `cmd[0]` and only its `agents/*` layers declare an `executables` filter, so no `toolchains/*` layer could ever be picked for a build tool that is a child of the confined command; `BROKEN_LAYERS` names 10 more that must never be added, nine of them because they carry SBPL symbols isol8's macOS backend does not define and one unbound symbol fails the whole policy; `DEV_RW_HOME_ROOTS` grants the dotnet paths by hand because isol8 ships no dotnet layer; and `TOOLCHAIN_ROOT_VARS` names the 17 environment variables that grant a relocated toolchain root. The pairing of the last one with `ENV_PASS` is the one part that cannot drift — `every_toolchain_root_var_is_also_passed_through` fails when a variable is added to one and not the other. The lists themselves still drift silently when isol8 changes — a layer renamed, a macro fixed, a dotnet layer added, a new toolchain's relocation variable — and nothing in the tree fails when they do | [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G187 | Docker cannot be enabled by naming `integrations/docker`: the shipped layer uses the undefined SBPL symbol `HOME_DIR`, so it is in `BROKEN_LAYERS` and any run that names it starts nothing. A confined agent that needs a container daemon reaches it only through hand-written extra grants for the socket and `~/.docker` — the same hand-replication xcode and chromium require, and the layer's own header calls socket access high-risk | [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G7 | The MCP surface Ubiq exposes to hosted agents is a module header. What tools it offers is undecided | [`tech/architecture.md`](./tech/architecture.md) |
| G10 | The chat has no transport family, so its composer sends to nothing and its reply is canned | [`features/chat.md`](./features/chat.md) |
| G11 | One of the rail modes — KB — renders an empty page, and the explorer and the chat leave with IDE mode. The terminals and the console stay, because a panel's visibility is its kind's rule rather than the frame's | [`features/workbench.md`](./features/workbench.md) |
| G12 | Ubiq ships no icon set, so the history and status glyphs borrow the nearest icon from the component library's bundle | [`tech/ui-and-design.md`](./tech/ui-and-design.md) |
| G14 | `just verify` is red on three clippy lints in `crates/agent-manager`, so the project gate cannot pass from a clean checkout | [`tech/operations.md`](./tech/operations.md) |
| G24 | `just test` is red on `codex_bridge_round_trips_events_and_terminates`: under the workspace run its `initialize` handshake times out after 10s, while the test passes on its own | [`tech/operations.md`](./tech/operations.md) |
| G16 | `⌘K` raises the navigator over the titlebar's command field, and its five groups of places — recents, bookmarks, files, tasks, agents — are built; Enter with the navigator shut is still the project content search. **Commands are the half that remains**, and they are `G137` | [`features/workbench.md`](./features/workbench.md) |
| G17 | The thinking-budget selection is recorded and never sent anywhere, like the rest of the composer's pickers | [`features/chat.md`](./features/chat.md) |
| G18 | No pane shows focus on its edge, because focus across split panes is designed ahead of the code. `selected` and `border_focus` are drawn on the file picker's keyboard cursor and nowhere in a pane | [`tech/ui-and-design.md`](./tech/ui-and-design.md) |
| G19 | Of the session family, only `SpawnWorkspace`, `WorkspaceSpawned` and `CloseWorkspace` are implemented (the project family is complete). `ListSessions`/`SessionList`, `CreateSession`/`SessionCreated`, `AttachToSession`/`SessionAttached`, `DetachFromSession`, `ListAgentTypes`/`AgentTypes`, `Status`, `Error` and the `SessionInfo` and `AgentTypeInfo` records exist in the transport contract document and in no code. The work family's `WorkSession` is a session on the wire, seen from the other side, and the two records merge when the session family lands | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G22 | Keyboard scrollback navigation is absent — Page Up/Down still go to the harness. The wheel in normal screen moves through scrollback | [`features/panes-and-terminals.md`](./features/panes-and-terminals.md) |
| G27 | Ubiq's four crates pin `serde`, `tracing` and `flume` independently; the workspace has no `[workspace.dependencies]` table to make a skew impossible | [`tech/project-structure.md`](./tech/project-structure.md) |
| G28 | The config root holds an agent's run directories, its preference templates and its isolation state, and not the library's catalogue, accounts or credentials — those keep their own roots, so a development run is self-contained in what a pane writes and not in what it reads | [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G29 | Two hosts on one config root are last-writer-wins over `projects.toml`; an advisory lock around the read-modify-write would close it | [`tech/architecture.md`](./tech/architecture.md) |
| G30 | Health is probed at load, on open and on request. The filesystem watch reports what changed inside a project's folder and never re-probes the record, so a folder that goes away is still only noticed the next time somebody asks | [`features/workbench.md`](./features/workbench.md) |
| G31 | A project binds to no profile. A profile is picked per conversation, from the New agent form's target picker, and nothing records that this project's agents should start as one — so every window asks the same question again. A default per project needs the id on `ProjectRecord` and a preference surface to set it from | [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G32 | Add, Locate and a clone's destination all open the platform's folder dialog, which browses the interface's filesystem — the one place the two halves are assumed to share a machine. A detached host needs a host-side listing behind it | [`tech/decisions.md`](./tech/decisions.md), [`features/workbench.md`](./features/workbench.md) |
| G26 | The log ring lives in memory and is not written anywhere, so diagnostics die with the process and a bug report cannot carry them | [`features/logs.md`](./features/logs.md) |
| G33 | The set of folders a deep walk will not descend into is fixed in `crates/ubiq-host/src/files/mod.rs`. Reading a project's `.gitignore` instead is what the interface's users will expect, and is the one thing that would justify the `ignore` crate | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G35 | `Row::loading` and `Row::truncated` are carried through the tree and drawn nowhere, so a folder whose listing is in flight looks empty and one cut short at the host's entry ceiling looks complete | [`features/workbench.md`](./features/workbench.md) |
| G36 | The file worker is one thread, so a request against a hung mount holds up the ones behind it. A pool would fix that and reorder the replies, which needs a sequence number on the wire before the interface could trust them | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G37 | A listing joins a child's path with a forward slash, which is the interface's own path shape and not Windows'. Nothing has been run there | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G38 | `spawn_workspace` looks a record up, probes its folder and resolves a path on the coordinator's thread, which every pane's keystrokes pass through. The file family was moved off that thread and the spawn path was not | [`features/panes-and-terminals.md`](./features/panes-and-terminals.md) |
| G39 | The chat belongs to the window rather than to a project, so switching projects keeps the conversation. It moves when the chat gets a transport family and a conversation is about something | [`features/chat.md`](./features/chat.md) |
| G40 | `FileLanguage` has no JavaScript arm, so a `.js` or `.mjs` file opens unhighlighted. It needs a variant and the matching arm in `ui/editor.rs`'s highlighter mapping | [`features/workbench.md`](./features/workbench.md) |
| G41 | `crates/ubiq-host/src/files/path.rs` is named in no document's `code_anchors`, so a change to the one security boundary the file family has is told it owes no document an update. The walk, the read, the save and the diff are anchored by [`tech/architecture.md`](./tech/architecture.md) | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G42 | `just docs-lint` is red on thirteen failures in `_docs/inbox/`, so the project gate cannot pass from a clean checkout for a third reason beyond `G14` and `G24`. Eleven are proposals no `INDEX.md` entry links, one has no frontmatter, and two are over the length ceiling | [`INDEX.md`](./INDEX.md) |
| G53 | `crates/ubiq-host/src/store/` is named in no document's `code_anchors`, so a change to a trait, to the on-disk format or to what a corrupt file does is told it owes no document an update. The same shape as `G41` | [`tech/architecture.md`](./tech/architecture.md) |
| G46 | The catalogue, the view state and a project's tasks are all written from the coordinator's run loop, which every pane's keystrokes pass through, so a `--config-root` on a network mount blocks them behind the write. This is the catalogue's own accepted pattern rather than a new defect — `G38` is about the *spawn* path probing a user's folder — and it holds for three stores | [`tech/architecture.md`](./tech/architecture.md) |
| G47 | The host answers work messages and never pushes one, so nothing tells a window that an agent moved on its own. A live agent needs an unsolicited variant the work family does not have | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G48 | The mock's five session ids and eleven agent ids are the same literals in every project, because a mock session is not a host object and nothing keeps a catalogue of them | [`features/sessions-and-workspaces.md`](./features/sessions-and-workspaces.md) |
| G49 | A step cannot be handed to an agent. `Step.owner` is on the record and read when a step is drawn, and nothing sets it | [`features/workbench.md`](./features/workbench.md) |
| G50 | Steps cannot be reordered from the interface, though `MoveStep` names a step and the place it should end up in on the wire for exactly that | [`features/workbench.md`](./features/workbench.md) |
| G51 | `cmd-s` and `ctrl-s` are bound in `crates/ubiq/src/app/mod.rs` to a `"Workbench"` key context no element declares, and `save_active_file()` is never registered with `cx.on_action`, so the keystroke does nothing and the save path cannot be reached from the keyboard | [`features/workbench.md`](./features/workbench.md) |
| G55 | The component library's dock places edge regions left, right and bottom only, so there is no top region: "docked on top" is a split at the top of the centre, and takes its width from the centre rather than spanning under the explorer | [`features/workbench.md`](./features/workbench.md) |
| G56 | A panel dropped in a region its class forbids is moved back on the same edit rather than refused under the pointer, because the library's drop is region-blind. The drop reads as refused; the drag offers no indicator saying it would be | [`features/workbench.md`](./features/workbench.md) |
| G57 | Panels cannot be moved between, or focused from, the keyboard. `D17`'s reversal makes it possible and nothing builds it | [`features/workbench.md`](./features/workbench.md) |
| G58 | The library's free-floating tiles canvas is reachable — Ubiq names a tiles renderer because a dock renderer must — and nothing builds one, so the renderer draws nothing | [`tech/ui-and-design.md`](./tech/ui-and-design.md) |
| G59 | A panel name a saved layout carries that this build does not know is dropped rather than kept as a placeholder, so a layout written by a later build loses those panels on the round trip. The version check catches the case that matters; a hand-edited file is the one that does not | [`features/workbench.md`](./features/workbench.md) |
| G68 | The file picker is raised by one screen — the kitchen sink's picker page — over a fixture tree, so no screen chooses a real path through it yet. It takes the forest it draws rather than fetching one, so a caller over a real project needs `ProjectTree`'s listings folded into `PickerNode`s and a second `PickerOwner` to route the answer to | [`features/workbench.md`](./features/workbench.md) |
| G60 | The session family's table documents `ListSessions`, `CreateSession`, `AttachToSession`, `DetachFromSession`, `ListAgentTypes`, `SessionList`, `SessionCreated`, `SessionAttached`, `AgentTypes`, `Status` and `Error`, none of which are variants of `Message` — the table is ahead of `crates/ubiq-proto/src/messages.rs`, which `G19` reads from the other side | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G61 | A diff compares the blob as it is stored with the file as it is on disk, so a repository using `core.autocrlf` or a clean/smudge filter shows a whole-file change where git shows none. Running the filters means running programs a merely-opened folder configures, which is why the host does not | [`tech/architecture.md`](./tech/architecture.md) |
| G62 | A path in the middle of a merge has no stage-zero entry in the index, so `DiffBase::Index` answers it as wholly added rather than saying it is conflicted. The contract has no conflicted state for a `FileDiff` to carry | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G63 | A diff is answered only when it is asked for. Nothing tells a window that a pane's agent changed a file it is showing a diff of, and the filesystem watch that re-lists the tree and refreshes git leaves an open diff alone | [`features/workbench.md`](./features/workbench.md) |
| G64 | `AppState::open_diff()` opens a tab on a file's change and asks the host for it, and nothing calls it: the explorer's rows have one click and no modifier reading, so a comparison can only be reached from a saved arrangement that held one | [`features/workbench.md`](./features/workbench.md) |
| G65 | A dirty file panel closed from its tab returns to the first group of the centre to ask, not to the group and the position it was closed from. The dock takes a closed tab out before the window hears about it, so the question is asked by putting the panel back | [`features/workbench.md`](./features/workbench.md) |
| G52 | The inspector's composer sends into a thread nothing answers. `SendToAgent` reaches the host, which appends the line to the mock agent's thread and answers with the agent carrying it, and nothing generates a reply — a reply needs a live harness behind the mock | [`features/workbench.md`](./features/workbench.md) |
| G66 | Nothing bounds the interface's workarea. The host reserves `projects/<ulid>/ui/` and never looks inside, so no size ceiling, no eviction and no age limit exists anywhere — the only thing that empties it is forgetting the project, which removes the directory wholesale | [`tech/architecture.md`](./tech/architecture.md) |
| G67 | The workarea is reserved by a `create_dir_all` inside `Projects::snapshot`, so every listing of the catalogue makes one syscall per project on the coordinator's run loop, beside the health probe on the same line. The same class as `G46` | [`tech/architecture.md`](./tech/architecture.md) |
| G68 | File search on single file (proposal in inbox) |
| G107 | Openers other than the search panel's rows still throw a position away. `navigate` with a `Locus::Line` opens a file at a line — `app/nav.rs::reveal_ide` applies it, or stashes `pending_goto` until the bytes land — and the search panel's file and hit rows use it. Every other opener calls `select_file()` and lands on line 1: `app/wire.rs::receive_file`, `app/boot.rs`'s startup path, and the explorer's six sites. Each wants a different locus, or none, so this is a row per caller rather than one change | [`inbox/omni-search-proposal.md`](./inbox/omni-search-proposal.md), [`inbox/ui-routing-proposal.md`](./inbox/completed/ui-routing-proposal.md) |
| G108 | Search is invisible outside IDE mode. `PanelKind::Search::is_drawn` is `is_ide && has_project`, so `⌘⇧F` and the titlebar's search icon add the panel to the dock and the next settling pass immediately sets it invisible — from Agents mode the gesture does nothing a user can see. Either the rule widens or the gesture switches the rail first | [`features/workbench.md`](./features/workbench.md), [`inbox/omni-search-proposal.md`](./inbox/omni-search-proposal.md) |
| G109 | The search filter and the external fallback are in the host and no document designs them. `Filter { patterns, subdir }` is honoured by the walk, per-project and global excludes are merged in the coordinator, and `search::fallback` runs a configured `grep` or `ag` when the matcher refuses a pattern — none of it appears below §0 of the proposal. The settings dialog now edits the two global lists; the per-project excludes on `ProjectRecord` and the per-search `Filter` still have no interface at all, so the panel sends `Filter::default()` every time | [`inbox/omni-search-proposal.md`](./inbox/omni-search-proposal.md) |
| G71 | excalidraw no code view |
| G72 | fix file selection on click no explorer |
| G73 | explorer file wrong spacing and disposition |
| G74 | explorer hide hidden files, and force hidden files and folder on selection |
| G75 | spacing on project selector |
| G78 | The catalog is not on the wire. The settings overlay's Harnesses section signs accounts in and defines profiles, and neither can name a skill, an MCP server, a hook or an instruction file, because nothing lists what the library's catalog holds. A profile form that offered them needs a catalog family beside the profile one | [`features/workbench.md`](./features/workbench.md), [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G79 | The titlebar's centre holds the command field on every screen, where the design shows the screen's own name — `Agents · parallel columns`. A per-mode title needs somewhere for the field to go, which is `G16`'s question from the other side | [`features/workbench.md`](./features/workbench.md) |
| G80 | A column's footer draws no mode chip, because `WorkAgent` carries no mode. The design shows one beside the harness and the model, so it needs a field on the wire before the footer can report it | [`tech/transport-contract.md`](./tech/transport-contract.md), [`features/workbench.md`](./features/workbench.md) |
| G81 | The agents sidebar's session note is the title of a task in that session, because `WorkSession` has no description on the wire. A session that is for something no task names has no line to draw | [`tech/transport-contract.md`](./tech/transport-contract.md), [`features/workbench.md`](./features/workbench.md) |
| G82 | `COLUMNS_MAX` caps the agents screen at eight columns because each owns a composer out of a fixed pool built before the first frame. An unbounded row needs a text area a column can be given when it opens | [`features/workbench.md`](./features/workbench.md) |
| G84 | The Git screen's fetch, pull, push, branch, stash, undo and commit are drawn and inert. Version control is read-only, so they wait on a write family and everything it needs — a confirmation surface, an undo, an answer for the agent in pane two who is mid-rebase, and a repository cache that can be written through: `crates/ubiq-host/src/git/mod.rs` shares one un-mutexed `Repository` per project, which is safe only while nothing mutates, and that shape has to change before the first write lands | [`features/workbench.md`](./features/workbench.md), [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G85 | A staged path's row is compared against HEAD rather than against the index, because the file family offers `DiffBase::Head` and `DiffBase::Index` and no index-against-HEAD. A staged-only comparison needs a third base on the wire | [`tech/transport-contract.md`](./tech/transport-contract.md), [`features/workbench.md`](./features/workbench.md) |
| G86 | The Git screen has no specimen on the kitchen sink and no wireframe under `design/`; it is built against `inbox/design/git-proposal.png`, which is raw material rather than a captured asset | [`tech/ui-and-design.md`](./tech/ui-and-design.md) |
| G87 | A shell pane's environment starts from Ubiq's own — `portable-pty` captures the process env at spawn — so a variable Ubiq was launched without reaches it only because the login shell's profile sets it. A composed agent carries the environment the library computed, and a confined one carries the whole of it; nothing computes one for a shell | [`features/panes-and-terminals.md`](./features/panes-and-terminals.md) |
| G89 | mouse scrolling direction in terminal should follow the os (for macos is inverted) |
| G90 | double click on the folder in the explorer opens an empy editor, it should be ignored |
| G91 | the markdown preview does not scroll anymore |
| G103 | The clipboard carries images in only: `read_from_clipboard`'s image arm opens an untitled picture through `⌘N` and paste, the `new_image` write path has no caller until copy-region lands, and `ClipboardEntry::ExternalPaths` moves in neither direction | [`inbox/shell-integration-proposal.md`](./inbox/shell-integration-proposal.md) |
| G104 | The explorer cannot drag a file out to the operating system, though `external_drag_payload` and `FileDragPaths` are available at the pinned gpui revision | [`inbox/shell-integration-proposal.md`](./inbox/shell-integration-proposal.md) |
| G105 | An external drop onto the explorer does nothing — no import, no move, no copy. The file family creates, moves and copies a path, so what is left is the gesture: the explorer's rows take a `DraggedPath` from inside the panel and no `ExternalPaths` handler, and an imported file has no message to carry its bytes in, since `EditProjectPath` names two paths inside one project and a dropped file is outside it | [`features/workbench.md`](./features/workbench.md), [`inbox/shell-integration-proposal.md`](./inbox/shell-integration-proposal.md) |
| G106 | An external drop onto the chat panel does nothing; there is no attachment gesture for a dropped file to become | [`features/chat.md`](./features/chat.md) |
| G110 | The filesystem watch reads its own ignore rules, not the walk's. `crates/ubiq-host/src/watch/mod.rs` builds one `Gitignore` from the project root's `.gitignore` plus the merged excludes: no nested `.gitignore`, no `.ignore`, no global excludesfile, and no skipping of hidden files the way `search::walk`'s `WalkBuilder` defaults do. `WalkBuilder` cannot answer a question about one path, which is why the two diverge; the cost is over-reporting — a re-listing that finds nothing new — and never a change missed. A per-directory stack of `Gitignore` matchers is the upgrade. [`wip/indexing.md`](./wip/indexing.md) says the index and the search cannot disagree about what a project's files are, which holds — both run `search::walk` — but the *watch* is the third reader and does not. The git worker's `StatusOptions` (`git/observe.rs:267`) is a fourth, via libgit2's own ignore rules rather than either `Gitignore` build | [`wip/indexing.md`](./wip/indexing.md), [`features/workbench.md`](./features/workbench.md) |
| G111 | A file that changed on disk while its tab holds unsaved edits is left silently alone — no `changed on disk` badge, no reload offer, no marker on the tab. The user learns of the collision only when the save is refused | [`features/workbench.md`](./features/workbench.md) |
| G112 | A reloaded tab flickers through its loading state and loses its undo history, because `OpenFile::reload()` drops the buffer entity rather than splicing the new bytes into the live one | [`features/workbench.md`](./features/workbench.md) |
| G113 | There is no `CloseProject` on the wire, so a watch is stopped by the same window opening a different project or by the window going away. A window that closes a project without opening another keeps a watch on a folder nothing in it shows | [`tech/transport-contract.md`](./tech/transport-contract.md), [`tech/architecture.md`](./tech/architecture.md) |
| G115 | `AppState`'s diagram and viewport caches (`diagrams`, `viewports` in `crates/ubiq/src/app/mod.rs`, filled by `crates/ubiq/src/app/chat.rs`) are insert-only: nothing ever removes an entry. A window accumulates one per diagram rendered and one per theme switch for as long as it stays open | [`features/chat.md`](./features/chat.md) |
| G116 | A conversation's transcript (`Conversation::blocks` in `crates/ubiq/src/state/conversation.rs`) grows without bound for the life of the chat — it can hold full diff bodies, and nothing ever trims or pages it out | [`features/chat.md`](./features/chat.md) |
| G117 | Off a macOS bundle the `ubiq` command execs the binary, so a second application starts with a catalogue of its own instead of the path reaching the window that is up. The single-instance handoff — a socket in `crates/ubiq-app` a live instance answers — is unbuilt, and `script()` in `crates/ubiq-host/src/cli_shortcut.rs` marks the spot | [`features/workbench.md`](./features/workbench.md), [`tech/decisions.md`](./tech/decisions.md) |
| G118 | `login_confined`'s real-home runtime grants (`crates/agent-manager/src/isolate.rs`'s `login_runtime_grants`) are unverified for opencode and grok — neither binary is installed on this machine, so their login flows have not been run under a relocated `$HOME` to confirm the grant list actually covers them | [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G119 | A conversation cannot be renamed from the UI once started, and three writers may set the name it cannot override: it starts mechanical (the harness's command, with a per-project counter, `D58`), the harness may replace it with a `ConvUpdate::Title`, and Ubiq may replace it with a reading of the opening exchange (`D90`) — none of the three is the user, and no rename message exists on the wire for one. A `RenameConversation { agent_id, name }` and a field that says a name was typed, so a later naming does not overwrite it, is the shape | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G120 | Claude Code's native bridge resumes its memory and redraws no transcript. `--resume` hands the harness back its own session, so a restored conversation answers in context, but `stream-json` replays nothing — where every ACP harness re-emits its whole history through `session/load`, a `claude-code` tab comes back live above an empty transcript. The record to redraw from is in the kept run directory, in the format `io/jsonl.rs` parses (`<config dir>/projects/<slug>/*.jsonl`), so the missing half is a reader over a file rather than a pipe, emitted ahead of the fresh `Started`. It depends on `ConvUpdate::Compacted` being drawn first: the file holds pre-compaction history the harness does not carry | [`tech/transport-contract.md`](./tech/transport-contract.md), [`features/sessions-and-workspaces.md`](./features/sessions-and-workspaces.md) |
| G121 | `Conversation::stop` joins the pump thread on the coordinator's own thread. An `UnloadConversation` or `EndConversation` whose bridge will not drain — a harness wedged mid-write, ignoring cancel — blocks that join, and the coordinator answers every window from one thread, so one stuck harness freezes every window's pane and conversation traffic until it gives up. `AbortConversation` is the escape rather than the fix: it kills the process before the join, so the join it does is short, but a user has to ask for it and `EndConversation` still waits | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G122 | A column's `+` menu groups agents by availability alone — the bench versus what is already on screen elsewhere — because `WorkAgent::role` and `::task` are mock values today, filled by nothing real. A profile does not fill them either — it pins a harness, an account, a model and a mode, and carries no role and no task — so grouping by role, task or team waits on those fields existing on a definition at all | [`features/workbench.md`](./features/workbench.md), [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G124 | A log with no `rel_path` walks the whole repository, not the project's own prefix. A monorepo's history view shows every other project's commits until the caller narrows it by hand | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G125 | The overview does not account for linked worktrees or a repository nested inside another. `Repository::discover` finds the nearest `.git` and stops there, so a project inside a linked worktree or a repository-in-a-repository is read as if it were the only one | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G126 | `GitError::Interrupted` is never constructed. libgit2's status walk has no interrupt to report one from, so the variant exists on the wire for a future cancellable walk and nothing sends it today | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G127 | `GitRefs` and `GitLogPage` carry no `generation` field — `GitWorkingTree` and the overview reply do, and the interface discards an older one on arrival. `crates/ubiq-host/src/git/mod.rs`'s `Request::Refs` and `Request::Log` arms answer unconditionally, with no staleness guard at all, so an in-flight refs or log reply from a superseded request still lands on top of the ones after it | [`tech/transport-contract.md`](./tech/transport-contract.md), [`features/workbench.md`](./features/workbench.md) |
| G130 | `crates/ubiq-host/src/files/diff.rs::diff()` calls `Repository::discover` per request, uncached, while the git worker (`git/mod.rs:86`) keeps one `Repository` per project. `D43` accepted a second comparison engine, not a second discovery walk paid on every file the user opens | [`tech/architecture.md`](./tech/architecture.md), [`tech/decisions.md`](./tech/decisions.md) |
| G131 | The project catalogue's health probe (`crates/ubiq-host/src/health.rs::probe`) is filesystem-only — `symlink_metadata` and a directory check, nothing about `.git`. A corrupt `.git` probes healthy while the git worker returns `GitError::Corrupt` to a different part of the screen: two truths, no reconciliation | [`features/workbench.md`](./features/workbench.md), [`tech/architecture.md`](./tech/architecture.md) |
| G132 | `submodule_state` (`crates/ubiq-host/src/git/observe.rs:159`) returns `GitSubmoduleState::Clean` when `repo.submodule_status` errors, so a broken submodule reports as a healthy one — a silent wrong answer rather than an absent one, and the only place in the git family that does that | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G133 | The four git ceilings — `MAX_WORKING_TREE` (`ubiq-proto/src/git.rs:21`, 2 000 entries), `AHEAD_BEHIND_CAP` (`:18`, 99), `MAX_LOG_PAGE` (`:207`, 200 commits) and `PATH_SCAN_CEILING` (`git/history.rs:15`, 5 000 commits scanned) — are honest bounds with a `truncated` story behind them, and none is measured against a repository of the size Ubiq is actually opened on | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G134 | An agent's turn and the commit it produced are unconnected facts. `crates/ubiq-host/src/work/` and `crates/ubiq-host/src/links.rs` hold no git reference, and no message joins a conversation turn to a commit id. Ubiq is the one application in the category watching both the agent and the repository, and it joins neither — an unclaimed opportunity rather than a defect, and the reason to keep the log family while no screen pages it | [`tech/version-control.md`](./tech/version-control.md), [`features/workbench.md`](./features/workbench.md) |
| G135 | The git worker's per-project state — the repository cache, the generation counter and the lane cache in `crates/ubiq-host/src/git/mod.rs` — is keyed by project id with no client awareness. The git family is addressed to the asker rather than broadcast, which rides on "a project is open in exactly one window"; that invariant is enforced by the workbench, so this is an untested seam rather than a known bug, but nothing in the worker would notice if two clients ever shared one project's handle and one generation | [`tech/version-control.md`](./tech/version-control.md), [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G136 | A path a chat message merely *mentions* is text, not a link. `ConvBlock` (`crates/ubiq/src/state/conversation.rs`) is plain strings, so the transcript's rendered replies carry only the links a harness wrote as Markdown. Blocked on the conversation family carrying real spans on the wire; scanning prose for paths is the alternative, and it is the rule about not reading a harness's output one surface over | [`features/chat.md`](./features/chat.md), [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G137 | The `⌘K` field's command half. The navigator offers five groups of places; a command is not a place, and a command row needs a second kind of row action, an action registry, a name and a keystroke per action, and a pass over every action in `crates/ubiq/src/app/mod.rs`. Deferred until a second consumer of an action catalogue exists — a keymap settings page is the likely one | [`features/workbench.md`](./features/workbench.md) |
| G138 | `Locus::Anchor` cannot be honoured inside the Markdown preview: `TextView`/`TextViewState` expose no scroll-to-anchor, heading id or block index at the pinned `gpui-component`. An anchor resolves against the **source** instead — `crates/ubiq/src/state/nav.rs::line_of_slug` scans for the heading and lands as a line on the editor side — so in a preview-only layout the locus is dropped | [`features/workbench.md`](./features/workbench.md), [`tech/ui-and-design.md`](./tech/ui-and-design.md) |
| G139 | The dock exposes no focused-panel reader — `crates/ubiq/src/ui/dock/mod.rs` has `holds`, `add`, `reveal` and `remove`, and nothing that answers which panel holds the keyboard. So `View::Terminal`, `View::Logs` and `View::Chat` can be linked to and revealed, but `current_destination` can never return one: they are absent from the history and never the place a bookmark is taken of | [`features/workbench.md`](./features/workbench.md) |
| G140 | Mouse back and forward buttons are not bound to `NavBack`/`NavForward`. Whether gpui reports the extra mouse buttons at all is unchecked | [`features/workbench.md`](./features/workbench.md) |
| G141 | A cross-project bookmark has nowhere to live. Bookmarks ride the project's own preference blob under `Scope::Project` (`D64`), so a place in project A cannot be written down from project B, and forgetting a project forgets its bookmarks by design | [`features/workbench.md`](./features/workbench.md), [`tech/decisions.md`](./tech/decisions.md) |
| G142 | The sign of `ScrollHandle::offset()` when the graph is scrolled is unverified against a real scroll. `where_locus` writes it into a `Viewport` locus and `reveal_graph` hands it straight back to `set_offset`, so a sign error round-trips invisibly through a link and only shows as a graph that lands in the wrong place | [`features/workbench.md`](./features/workbench.md) |
| G143 | A bookmarked line is marked on the line, not in the gutter. `gpui-component`'s only public decoration surface is `TextDecorationCollection` over byte ranges; the gutter is drawn in a private element hard-coded to line numbers and fold icons. A gutter mark needs an upstream change | [`tech/ui-and-design.md`](./tech/ui-and-design.md) |


| G144 | Connector tokens are never refreshed on use. A stored `refresh_token` and expiry are kept, and `credential_validity` reads the expiry, but nothing exchanges one — an expired OAuth connection reads `Expired` and is reconnected by hand | [`features/connectors.md`](./features/connectors.md) |
| G145 | No connection is read by anything. The deliverable is a valid token and the identity it belongs to; no git remote, task source or document picker takes a connection id yet | [`features/connectors.md`](./features/connectors.md) |
| G146 | A pinned certificate is the leaf's fingerprint, so a renewal re-prompts. Pinning the SPKI would survive one, and is the upgrade if re-confirming annoys anyone | [`features/connectors.md`](./features/connectors.md) |
| G147 | A pasted secret is not masked: `InputState` has no masked mode in the component kit, so a connector's token is on screen until the connect modal closes, and an API provider's key is on screen until the provider form closes | [`features/connectors.md`](./features/connectors.md), [`features/workbench.md`](./features/workbench.md) |
| G148 | A corrupt `host-settings.toml` reads as defaults, so the next connector write starts from an empty list and the connections it held are gone. True of the other host settings too; it costs more here | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G149 | A clone runs over https only. `parse_repo_url` recognises a `git@…` remote and normalises it to the https URL its owner would have pasted, so the modal can name ssh as unsupported rather than say "not a repository", and the clone refuses it. An ssh clone needs a credential callback, an agent socket and a key the host has no story for | [`features/workbench.md`](./features/workbench.md), [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G150 | A failed clone is not resumed or retried. `CloneFailed` ends the flow, the partial destination is removed, and the only way forward is to ask again from the top — which is right for a refused credential and wasteful for a network that dropped at ninety percent | [`features/workbench.md`](./features/workbench.md) |
| G151 | Repository discovery is what the provider's membership listing returns, paged to a fixed ceiling, plus the provider's own search when a filter comes up empty against a truncated one. There is no org-wide or group-wide enumeration, so a repository the identity can read but is not a member of is reachable only by pasting its URL | [`features/workbench.md`](./features/workbench.md) |
| G152 | Azure DevOps, Atlassian and Google Workspace connections have no repository listing behind them and answer `CloneError::Unsupported`. Three of the six providers, so a connection to one of them buys nothing yet | [`features/connectors.md`](./features/connectors.md), [`features/workbench.md`](./features/workbench.md) |
| G153 | The outline parses the buffer a second time. `gpui-component`'s `SyntaxHighlighter` holds a tree-sitter tree with a public `tree()` accessor, but it sits inside a private `TreeSitterInputHighlighter` behind `Box<dyn InputHighlighter>` with no downcast, and `EditorState::highlighter()` is `pub(super)` — unreachable outside the crate, so `ui/outline.rs` parses the same bytes again with its own grammar set. Goes away the day `InputHighlighter` grows a tree accessor | [`features/workbench.md`](./features/workbench.md), [`tech/ui-and-design.md`](./tech/ui-and-design.md) |
| G157 | A client secret stored before registrations were named is filed under `connector-app:<provider>` and its origin, and nothing reads that key any more: the secret of an upgraded registration has to be entered again. The rows themselves migrate, the secrets do not | [`features/connectors.md`](./features/connectors.md) |
| G158 | A client secret typed into the registration form for a registration that does not exist yet is matched back to its record by name and client id once the host has minted an id, since the interface has nothing else to match on. A concurrent save of an identically named registration in another window would file the secret against the wrong row | [`features/connectors.md`](./features/connectors.md) |
| G159 | `ClientIdSource` names which registration answered and nothing draws it: the connectors list says a registration exists, not which one a given connection authenticates as | [`features/connectors.md`](./features/connectors.md) |
| G161 | The meter's message columns have no source. `msgs_in`, `msgs_out` and `tool_calls` are written as zero on every row: the conversation pump sees chunks and tool-call patches, not the per-turn counts a spend report belongs to. Codex, Copilot and opencode report no usage at all, so they contribute no rows either | [`features/stats.md`](./features/stats.md), [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G162 | The orchestration graph's parent edges are fixture-only. `WorkAgent::parent` (`crates/ubiq-proto/src/work.rs`) is filled by nothing real — the only writes are `crates/ubiq-host/src/work/mod.rs`, which clears it, and the mock in `work/mock.rs` — so `crates/ubiq/src/ui/orchestration/graph.rs` draws a connector for mock agents and never for a live one. The attribution itself is in hand: `parent_tool_use_id` is mapped end to end into the proto `Subagent` by `crates/ubiq-host/src/conversation.rs`'s `map_origin`, so what is missing is a real subagent becoming a `WorkAgent` with its spawner named | [`features/workbench.md`](./features/workbench.md), [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G163 | The usage meter has no cost column. `UsageRow` (`crates/ubiq-proto/src/stats.rs`) counts tokens and messages and nothing else, so what a turn cost is live-only — `UsageRecord::cost_usd` reaches the footer of the conversation that spent it and is never persisted or aggregated, which `crates/ubiq/src/ui/stats.rs` acknowledges. A spend report over sessions needs the column on the row, the bucket's upsert accumulating it, and the harnesses that report no cost at all answering with nothing rather than zero | [`features/stats.md`](./features/stats.md), [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G164 | A conversation's record is deleted on a timer and by an explicit end, and by nothing a user can see. `gc::collect_conversations` takes an unmarked conversation's record after `HostSettings.retain_conversations_days`, and `EndConversation` takes run directory, row and session record together — but no surface lists what is kept, so a persistent conversation the user stops wanting can only be reached by finding its tab. `crates/agent-manager/src/cli/session.rs` still offers `ls`, `show` and `resume` and no `rm`. A surface listing conversations, with a delete on it, is the missing half — and it needs `G208` first | [`features/sessions-and-workspaces.md`](./features/sessions-and-workspaces.md), [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G165 | A profile cannot be deleted or renamed. `FsProfileStore` offers `save` and `profiles()` and no `delete`, so the profile family has no message for either and saving over an id is the only correction available — a mistyped name is permanent from the interface. The fix is a library `delete`, not a `remove_dir_all` in `crates/ubiq-host/src/agent.rs`, which would be rule 1 of [`tech/agent-manager.md`](./tech/agent-manager.md) crossed. The same shape as the account rename and delete that do exist | [`features/workbench.md`](./features/workbench.md), [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G168 | The listener (`crates/ubiq-host/src/remote.rs`, started by `ubiq-app --serve`) runs plaintext: there is no TLS, so a remote attach is readable by anything between the two machines. Authentication is the token alone — a bearer string printed once at startup, with no rotation, expiry or per-client identity behind it, so anyone who obtains it has the same terminal access as the machine's own user for as long as the process runs | [`tech/architecture.md`](./tech/architecture.md), [`tech/transport-contract.md`](./tech/transport-contract.md), [`tech/decisions.md`](./tech/decisions.md), [`tech/operations.md`](./tech/operations.md) |
| G169 | Application settings' Hosts section gives a window a saved-hosts list, a dropdown to tell attached hosts apart, and a per-row Disconnect that runs the same teardown a dropped socket does (`crates/ubiq/src/app/hosts.rs`'s `host_menu_rows`, `crates/ubiq/src/ui/settings.rs`'s `hosts_section`), so the gap this row once named is closed. Two smaller ones remain: a saved host (`ubiq_proto::settings::SavedRemoteHost`) is named after the address it was first reached at, and renaming is not offered, only forgetting; and its token is never stored, so every reconnect — from this list or from the titlebar modal — re-prompts for it, with no way to skip that for a host reached often | [`features/workbench.md`](./features/workbench.md), [`tech/architecture.md`](./tech/architecture.md), [`tech/decisions.md`](./tech/decisions.md) |
| G171 | An attachment is an `@path` mention rather than the file itself. The composer's tags carry a project-relative path and its size, and `Conversation::compose_prompt` writes them into the one `PromptAgent` text — so a harness that does not resolve `@path` gets a string it can only guess at, and the size the tag warns about is never the size of anything sent. `agent-manager` accepts a `ResourceLink` content block (`crates/agent-manager/src/io/model.rs`), so the missing half is on Ubiq's own wire: `PromptAgent` carrying content blocks instead of one string, and the host reading the file rather than the interface naming it | [`features/chat.md`](./features/chat.md), [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G170 | A pending IME composition is not drawn in a pane. `TerminalView` holds the marked text so macOS composes a dead key correctly, but nothing paints it, so the accent is invisible between `` ` `` and `e` — and a CJK pre-edit is invisible for its whole length. Painting it at the cursor in `vendor/gpui-terminal/src/render.rs` is what closes it | [`features/panes-and-terminals.md`](./features/panes-and-terminals.md) |
| G172 | The commit-message subject has no sender. `crates/ubiq-proto/src/assist.rs` carries the subject, the reason set, the limits, the provider set and the API provider records; `crates/ubiq-host/src/assist/` carries the `Assist` trait, the macOS FoundationModels backend behind the `assist-apple` feature, the stub every other build gets, and the three-protocol API backend; `HostSettings.assist` is the setting and the settings overlay draws its section, the provider list and the streaming test modal. Two call sites exercise that path: the settings modal's `SuggestSubject::ProviderCheck`, which is a self-test and names nothing a user wanted written, and the automatic conversation naming, which is the coordinator's own and rides no subject at all (`D90`). **`SuggestSubject::CommitMessage` is the variant with nothing behind it**: the commit box's button in `crates/ubiq/src/ui/git/changes.rs` is where it would be sent from, it needs no new wire message, and `crates/ubiq-host/src/assist/subject.rs` holds the wording for it. The mechanical namings still further out are a session taking its project's name (`G184`) and an unattached chat tab reading `New chat` | [`tech/transport-contract.md`](./tech/transport-contract.md), [`tech/architecture.md`](./tech/architecture.md), [`features/chat.md`](./features/chat.md) |
| G183 | A generated conversation name is written down only for a conversation somebody marked. The naming is built — the host reads the opening exchange and pushes one `ConversationNamed` (`D90`) — and the title lands on the durable row, so a marked conversation comes back named. An unmarked one still has nowhere to keep it: `WorkAgent.name` and `WorkAgent.summary` are per-request payloads, so its name dies with the window that heard it. Whether that is worth fixing separately, or is simply what not marking a conversation means, is the open half | [`tech/transport-contract.md`](./tech/transport-contract.md), [`features/workbench.md`](./features/workbench.md) |
| G184 | A suggested session note has nowhere to go either: `WorkSession` carries no description field on the wire, which is the same gap `G81` names from the sidebar's side. A subject for it is worth adding once the field exists, and pointless before | [`tech/transport-contract.md`](./tech/transport-contract.md), [`features/workbench.md`](./features/workbench.md) |

| G173 | Wayland has no screen-capture backend in this dependency graph, so `cx.is_screen_capture_supported()` answers false and the titlebar's capture button is absent there — the image editor is still reached by pasting and by opening a file, which is a stated answer rather than a defect. What is open is whether a backend through `xdg-desktop-portal` is worth proposing upstream in gpui, or whether Wayland stays a platform where Ubiq does not capture | [`features/workbench.md`](./features/workbench.md) |
| G174 | Windows draws a capture border round a Graphics Capture source, and `crates/ubiq/src/app/capture.rs` crops a display frame rather than owning the source's configuration, so the border lands inside the picture. Whether it can be suppressed on the versions Ubiq supports, and what a window shot looks like where it cannot, is untested — nothing in the tree has been run on Windows against a live capture | [`features/workbench.md`](./features/workbench.md) |
| G175 | An `.excalidraw` document stays read-only although the machinery to edit one is built. `OpenFile::editable_image` gates on untitled *and* the image viewer, so the scene viewer never reaches `ImageEdit`, and the tools, the undo stack and the flatten would all have to learn a scene that is a file rather than a base image with elements over it | [`features/workbench.md`](./features/workbench.md), [`tech/diagram-format.md`](./tech/diagram-format.md) |
| G176 | An annotated capture saves as a flattened PNG and can be saved as nothing else. The scene is not writable because the base image is held whole behind the annotations — a rectangle over a token redacts it in the flatten and in nothing else — so saving the scene needs a flatten-what-is-covered rule first: which elements are opaque, and what happens to the pixels under them | [`features/workbench.md`](./features/workbench.md) |
| G177 | A capture cannot be sent to a conversation, which is the obvious reason to photograph a pane. No message in the transport carries an image: `PromptAgent` is one string, so this is a `ubiq-proto` question and a `crates/agent-manager` one — `G171`, the attachment gap, is the same missing shape on the wire | [`tech/transport-contract.md`](./tech/transport-contract.md), [`features/chat.md`](./features/chat.md) |
| G178 | `crates/ubiq/src/ui/viewer/image.rs`'s `format()` and `ViewerKind::of` both read the path's extension and never the bytes, so what draws a picture is decided by its name. The capture path works around it by naming its tab `capture-{n}.png`; sniffing magic bytes would make an image opened under the wrong extension draw rather than say it is not text | [`features/workbench.md`](./features/workbench.md) |
| G179 | A pasted image that is not PNG opens read-only. GPUI normalises and decodes every clipboard format but `Pnm` for display, while `ImageEdit::new` measures the base through the `image` crate, which is compiled with `png` alone here — so a macOS TIFF or a Windows DIB becomes a tab that draws and saves but never annotates. Either `image` gains the decode features, or the base is measured through GPUI's decoder | [`features/workbench.md`](./features/workbench.md) |
| G180 | Whether `⌘⇧2` is free of the component library's own bindings is unverified, as is GPUI's precedence for a binding registered after `gpui_component::init` — `CaptureWindow` is registered at `"Workbench"` depth and appears to win, on one platform, by observation rather than by a rule anyone has read | [`features/workbench.md`](./features/workbench.md) |
| G181 | Two things the image editor's tool set gives up. Blur and pixelate are the redaction tools a screenshot wants and they are pixel operations, which do not fit an element model where undo is a snapshot of a shape — they would need the flatten path at edit time, and `G176`'s rule with it. And the crop rectangle snaps to nothing: cropping to a pane or a panel is done by eye, though the window's own geometry is what the dock holds | [`features/workbench.md`](./features/workbench.md) |
| G182 | A resume under a changed home mode is refused rather than reconciled. The mode is on the conversation row, so a resume can tell that the setting moved under it — but all it does is decline, and the two questions behind that are open: what should happen to a run whose `Ephemeral` home is gone by definition, and whether a user who switched the application-wide setting expects their kept conversations to follow it or to hold the home they were started in | [`tech/agent-manager.md`](./tech/agent-manager.md), [`features/sessions-and-workspaces.md`](./features/sessions-and-workspaces.md) |
| G188 | Only the local host's `HostInfo` is applied. `AppState::receive_session` returns early for any other host, so the config root the status bar names, and the shells, agent types, accounts and profiles the New-pane and New-agent menus offer, are the local machine's whatever host is active — starting a pane on a remote offers that remote nothing of its own. Per-host menus need the four lists keyed by `HostRef` in `crates/ubiq/src/state/`, and every menu that reads them to say which host it is asking about | [`tech/architecture.md`](./tech/architecture.md), [`features/workbench.md`](./features/workbench.md) |
| G189 | A remote session has no keepalive and no heartbeat. `HANDSHAKE_TIMEOUT` covers the handshake alone and the session that follows is deliberately untimed, so a half-open connection — a laptop suspended, a NAT entry expired — is noticed only when a frame is next written and fails, which for an idle pane can be never. A periodic ping in `crates/ubiq-host/src/remote.rs`'s pump and its answer in `crates/ubiq/src/app/remote_connect.rs` is the shape, and it needs a message on the wire or a frame type below it | [`tech/architecture.md`](./tech/architecture.md), [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G191 | Two interface features read a host's absolute paths directly and so do nothing useful against a remote. The web-export server (`crates/ubiq/src/web_export/`) walks a project's folder with `std::fs` on the interface's machine (`D55`), and the diagram render cache writes into the project's `workarea`, which is a path on the host's machine — `Q10` asks what a remote host should mean for that cache; this row is the same question for the export, whose answer is likelier a host-side walk than a fallback root. `G32`'s folder dialog is the third instance of the pattern | [`tech/architecture.md`](./tech/architecture.md), [`features/workbench.md`](./features/workbench.md) |
| G192 | The on-device backend does not really stream. `Assist::stream` is defaulted, so macOS generates the whole answer and hands it over as one chunk — correct, and indistinguishable from a pause. The `foundation-models` crate does offer a blocking `stream`/`stream_with` taking an `FnMut(StreamEvent)`, which fits the host's threads-not-async model directly, so this is an override in `crates/ubiq-host/src/assist/apple.rs` and nothing else. It is unwritten because that file needs a Swift toolchain and the macOS 26 SDK to compile, which the round that added streaming did not have | [`tech/decisions.md`](./tech/decisions.md) |
| G193 | No API provider backend has been exercised against a live provider. `crates/ubiq-host/src/assist/api.rs` is covered by unit tests over fixtures — every endpoint, every header, every delta shape, every model list — and nothing has sent a request to OpenAI, Anthropic or Gemini. The three shapes are written from each vendor's documented wire format, so the failure modes left are the ones a fixture cannot show: a field renamed on one API (an OpenAI-compatible server that took `max_tokens` away), a keep-alive framing that is not `data:`, an error body that is not `error.message`. Testing it needs a key, which is a thing this repository must not hold | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G194 | A delegate's spend is banked per subagent **type**, so there are no per-instance token figures to draw. `UsageRecord::subagent` on the wire names the type a report belongs to, and `Conversation::spend_by_subagent` keys the buckets by it, so two `general-purpose` delegates sum into one — a delegate's footer says what that type spent and its tooltip says so outright rather than dividing a shared total to look exact. This is a protocol grain, not a UI omission: closing it means a subagent *instance* id on the usage report, from every harness that reports one at all | [`features/chat.md`](./features/chat.md), [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G195 | No harness reports a per-delegate context-window level, so a delegate's transcript draws no context ring. A subagent's usage report repeats the *parent's* occupancy, which is a reading about somebody else, so the ring is dropped rather than borrowed — the same rule `G96` follows for a harness that names no window at all. The gap is on the wire: `UsageRecord` would have to carry the delegate's own window and its own occupancy, and no harness Ubiq speaks to states either | [`features/chat.md`](./features/chat.md), [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G196 | Two `agent` tests fail on Windows only. Their fixtures write a tempdir path with single backslashes into a TOML string, which the parser reads as escape sequences, so `a_profile_s_account_is_what_composes_the_run` and `every_confined_run_keeps_the_real_home` in `crates/ubiq-host/src/agent.rs` fail while every other host test passes | [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G197 | The notification history and every mute rule are held in memory by `notifications::Centre`, so a host restart loses both — including a mute the user set for eight hours. The store traits under `crates/ubiq-host/src/store/` are the shape the fix takes: the rules are the half worth keeping, since a lost history is a bell that starts empty and a lost rule is Ubiq talking again when it was told not to | [`features/notifications.md`](./features/notifications.md), [`tech/decisions.md`](./tech/decisions.md) |
| G198 | Two of the four notifications worth wiring first are raised: a turn ending on a conversation nobody is looking at, and a permission a harness is waiting on, both from the `ConversationUpdate` arm in `crates/ubiq/src/app/wire.rs`. Two remain producer-less — a pane exiting non-zero, and a clone that failed — each one `NotificationRequest` at a site that knows it happened | [`features/notifications.md`](./features/notifications.md), [`features/chat.md`](./features/chat.md) |
| G199 | A notification linking to an agent lands on the project that window has on screen. `AppState::follow_notification_link` resolves `UbiqLink::Agent` through `open_task_chat`, which navigates within the current project, and nothing on `AppState` maps an agent back to the project it belongs to — so a notification about an agent in another project opens the wrong conversation or none. `UbiqLink::Pane` is the same shape one step milder: `focus_pane` focuses a pane whose panel may be hidden, where `navigate`'s own terminal arm would reveal it first. Both want the lookup, not a wider link | [`features/notifications.md`](./features/notifications.md), [`features/workbench.md`](./features/workbench.md) |
| G200 | Whether a notification reaches the desktop is decided one call site at a time: `os` is a flag on the request, so a subsystem author chooses it, and a user who wants every agent error as a toast and nothing else has no way to say so. A per-family default in `HostSettings`, ORed into the request the way a mute rule is, is the shape — and it is worth having only once `G196` has given the setting something to govern | [`features/notifications.md`](./features/notifications.md), [`features/workbench.md`](./features/workbench.md) |
| G201 | The orchestration graph's chosen arrangement (`state::layout::Algo`, `D89`) is not remembered. It lives on `GraphView` beside zoom, so it resets to `Flow` when the window is rebuilt and is never carried across windows or restarts — a user who works in `Columns` picks it again every time. The saved workbench arrangement is where it would go, which makes it a schema change rather than a field | [`features/workbench.md`](./features/workbench.md), [`tech/decisions.md`](./tech/decisions.md) |
| G202 | A naming thread has no deadline. `Coordinator::name_job` deliberately runs without one, unlike `suggest_job`: nothing is waiting on the answer, so a slow model was judged to cost a parked thread rather than a spinner that never stops. A provider that accepts a connection and never answers therefore parks that thread for the life of the process, one per conversation named, and the socket with it. A deadline on the generate call, or a reader timeout in `crates/ubiq-host/src/assist/api.rs`, is the shape | [`tech/transport-contract.md`](./tech/transport-contract.md), [`tech/decisions.md`](./tech/decisions.md) |
| G203 | A conversation is named once, from its first exchange, and nothing re-reads it. `PendingConversation::named` is set whether the naming answered or not, so a conversation that starts as one thing and turns into another keeps a title about where it started, and a provider configured *after* a conversation began never names it at all. What is missing is a trigger — a turn count, a subject drift the interface could notice, or a menu row that asks for a re-read — and any of the three needs a decision about what a second naming may overwrite, which is `G119`'s question from the other side | [`tech/transport-contract.md`](./tech/transport-contract.md), [`features/workbench.md`](./features/workbench.md) |
| G204 | `environment.toml` has no interface: `crates/ubiq-host/src/environment.rs` reads a machine's toolchain variables and grants, and the only way to write one is by hand, under a config root a user has to find first. A settings screen for it needs nothing new on the wire that `HostSettings.extra_grants` does not carry: the shape is a variables table and a path list — but the two files would need one form, since the whole point of keeping them apart is that a user thinks of a machine's environment as one thing | [`tech/agent-manager.md`](./tech/agent-manager.md) |
| G223 | The New agent form's persistence checkbox is drawn disabled. A conversation is marked from its own three-dots menu, which is the surface that exists once the row keyed by it does; the form's control would have to carry the intent through `StartConversation` and set the flag after the row is written. Until it does, a start cannot ask for a conversation to be kept — the user marks it after the first turn | [`features/workbench.md`](./features/workbench.md) |
| G224 | A fork is refused while its source's harness is running, which is broader than it needs to be. Copying a session store mid-append tears the last record, so the coordinator declines — but it has no reading of whether a *turn* is in flight, only whether the process is up, so a fork of an idle-but-loaded conversation is refused too and the user has to unload first. `Conversation` exposes no turn state; the fix is one, and then the guard narrows to it | [`features/chat.md`](./features/chat.md) |
| G208 | A conversation is undiscoverable to a window that did not start it. Conversation-family messages route `To::Client(owner)` and ownership is single-client, checked by `Coordinator::drives` on every drive, unload, abort and resume; no message enumerates conversations and none transfers or shares ownership. So `features/chat.md`'s contract that a tab is a view onto a host-owned conversation holds only for the window that asked for it, and a second window cannot learn the conversation exists. The same absence blocks reattachment from a window that did not start the conversation, and any surface listing what a restart had (`G97`) | [`features/chat.md`](./features/chat.md), [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G216 | A pinned tab is not reordered to the front of the strip. Pin only ever withholds a close — `WorkbenchPanel::closable()`, `ui::tab_menu::rows()` and `close_editor_tabs_filtered()` are the whole of it — because the component library's dock exposes no tab-reorder call for any of them to drive. A user reading a strip left-to-right for "what is protected" has to check each tab's mark instead | [`features/workbench.md`](./features/workbench.md) |

## Open questions — a decision nobody has made

| # | Question | Affects |
|---|---|---|
| Q1 | Do sessions survive a restart? If so, what is persisted — the arrangement, the folders, or the conversations too? The window's own arrangement does, and drops its terminal panels on load; whether a restored layout should offer to respawn the harnesses it dropped is the half this question still owes | [`features/sessions-and-workspaces.md`](./features/sessions-and-workspaces.md) |
| Q3 | Does a crashed harness restart automatically, on request, or never? | [`features/panes-and-terminals.md`](./features/panes-and-terminals.md) |
| Q4 | Which of Ubiq's two session meanings maps onto the library's resumable session, and where does the mapping live? | [`tech/agent-manager.md`](./tech/agent-manager.md) |
| Q5 | The bus is unbounded, so nothing blocks and nothing is dropped. Should the queue be bounded instead, and if it is, what goes: the oldest chunks, a coalesced screen, or the whole pane's backlog? | [`tech/transport-contract.md`](./tech/transport-contract.md) |
| Q6 | How does a subagent's pane show its parentage, and who decides where it opens? | [`features/panes-and-terminals.md`](./features/panes-and-terminals.md) |
| Q7 | Is scrollback owned by the emulator, or does the coordinator keep a buffer so a reattaching UI can be repainted? This one is load-bearing for detach | [`tech/architecture.md`](./tech/architecture.md) |
| Q8 | A detached coordinator cannot write into the window's log ring. Does it carry its records over the transport as a message, keep its own ring the console queries, or write to a file the console reads? | [`features/logs.md`](./features/logs.md) |
| Q10 | The workarea is an absolute path on the host's machine, and the interface uses it directly. A detached host on another machine hands over a path the interface cannot open — does the interface fall back to a root of its own, does the host learn to serve the directory over the bus, or does a remote host simply mean no cached renders? | [`tech/architecture.md`](./tech/architecture.md) |

## Deferred — decided to wait

| # | Item | Why it waits |
|---|---|---|
| D1 | Splitting the coordinator into its own process | The contract makes it cheap later; nothing today needs it |
| D2 | Harnesses on remote hosts | Same reason, one step further out |
| D3 | Windows support | The pseudo-terminal layer is cross-platform; nothing has been run there |
| D4 | Packaging and distribution | The application is run from source |
| D5 | Automated tests for the application's window | The pane path is covered end to end over the bus and the window registry is covered without a frame; driving `AppState` itself needs a headless window |
| D7 | A model Ubiq carries or reaches for itself, rather than one the OS provides — `candle` with a bundled GGUF, `llama-cpp-2`, `foundry-local-sdk`, or a local Ollama detected on loopback | Each answers `G172` where the OS does not, and each costs something the OS path does not: a 100–500 MB model download and a management surface, or a cmake and libclang build requirement, or a network client with its own port and trust question. `Assist` and its three backends are the whole preparation — one more arm in `assist::select` is what any of these costs on the seam — and the choice waits until the on-device path has a user — [`inbox/on-device-assist-proposal.md`](./inbox/on-device-assist-proposal.md) |
| D8 | An enforcing assist backend on Windows. `Microsoft.Windows.AI.Text.LanguageModel` is the right API and its `TextSummarizer` is literally this use case, and three things stand between it and a build: there is no Rust projection at any level of officialness — the `windows` crate covers OS metadata only, `microsoft/windows-app-rs` was archived in 2022, and every real version of the `windows-app` crate is yanked; Phi Silica is a Limited Access Feature whose unlock token binds to a Package Family Name, so calling it needs MSIX identity Ubiq does not have; and it needs a Copilot+ NPU or a discrete GPU | The re-evaluation trigger is dated: Microsoft's own documentation replaces Phi Silica with Aion Instruct in retail in November 2026, keeping the `LanguageModel` API, dropping the token requirement and running on CPU — which removes two of the three obstacles at once. Until then Windows gets the stub, and the stub is a stated answer rather than a failure mode: `AssistReason::UnsupportedPlatform` is what the panel reports, and every call site reads correctly against it |
| D6 | Registering `ubiq://` with the OS as an OAuth callback scheme | The scheme is the interface's own internal navigation URI, so registering it means one string meaning two things; loopback callbacks work without it — [`features/connectors.md`](./features/connectors.md), [D68](./tech/decisions.md) |
| G154 | A project's full-text index is rebuilt whole every time the project opens: `index::build` empties the directory first, because the walk writes over every file it finds but nothing visits a file *deleted* while the project was closed, and its document would survive as a candidate that cannot exist. Keying each document by the modification time and length the walk yields anyway would reuse what matched and delete only what the walk did not see. Worth doing when a cold start on a large project is slow enough to notice | [`tech/architecture.md`](./tech/architecture.md) |
| G155 | Two windows with the same project open run two watches, so every change reaches the index twice. Delete-then-add is idempotent, so the cost is a wasted read per duplicate rather than a wrong answer | [`tech/architecture.md`](./tech/architecture.md) |
| G156 | The indexing level offers "Full text + symbols" and the symbol half is not built, so picking `full` behaves exactly like `light` and nothing on screen says so. Either the option earns its name — a host symbol table, the two file-family messages, and the surfaces that read them, per [`wip/indexing.md`](./wip/indexing.md) — or it is withdrawn from both settings surfaces until it does | [`wip/indexing.md`](./wip/indexing.md), [`features/workbench.md`](./features/workbench.md) |
| G205 | Most of the icon registry is not drawn yet — `just icons-check` names what is left — so the concepts those rows stand for still read as text, a letter or a coloured dot: every harness draws the same asterisk, four pane lifecycle states share a dot, eleven tool kinds share six colours, the six notification families and the git change kinds are text, and `role_icon` falls through to the IDE's own icon for any role outside its four. Nine `IconName` variants also carry more than one meaning — `Network` alone means a branch, a remote host, a connector and the orchestration graph — which is what the `workbench` category's four icons break apart. The `tool` and `harness` categories are drawn and the rail's `Tasks` badge with them; nothing is wired into `crates/ubiq` yet, which needs Ubiq's own `IconNamed` enum and either an `AssetSource` or `Svg::data`. Draw the rest one category at a time per the `ubiq-icons` skill | [`tech/ui-and-design.md`](./tech/ui-and-design.md), [`features/workbench.md`](./features/workbench.md) |
| G206 | A2UI reaches no agent. The renderer is built and drawn on the kitchen sink's own page — `ui::a2ui` draws all eighteen components of the basic catalog from a payload, and `crates/ubiq/tests/a2ui.rs` holds it to that — but nothing produces one: no harness Ubiq hosts emits A2UI, and no message carries a surface across the bus. The MCP-first shape in [`inbox/a2ui-proposal.md`](./inbox/a2ui-proposal.md) is the way in, and its phase 0 is the host becoming an `McpService`. Two pieces the sink page deliberately leaves out are what a real surface needs: the data model behind a JSON Pointer binding, and the runtime id-to-entity map a text field that accepts a keystroke costs | [`references/a2ui-protocol.md`](./references/a2ui-protocol.md), [`features/workbench.md`](./features/workbench.md), [`features/chat.md`](./features/chat.md), [`tech/transport-contract.md`](./tech/transport-contract.md) |
| G211 | The interface hand-rolls widgets the library it embeds ships. `gpui-component` is initialised, its asset provider installed and every window wrapped in its `Root` — so its dialog, popover, menu and toast layers are live and unused — while `ui/kit` carries a fifty-three-site dropdown with no arrow-key navigation, a second context-menu body duplicating the first, a filterable list in `ui/navigator.rs` parallel to the picker's, a from-scratch grid in `ui/stats.rs`, and a modal stack with its own hand-numbered z-priorities. Its blocking task is closed: `theme::dress_component_library` writes Ubiq's tokens into `gpui_component`'s `ThemeColor` on every `set_theme`, so a library widget draws in Ubiq's colours and the consolidation itself is what is left. Ranked, with what to keep as house skin and what has no analogue at all, in [`inbox/component-reuse-proposal.md`](./inbox/component-reuse-proposal.md) | [`tech/ui-and-design.md`](./tech/ui-and-design.md), [`features/workbench.md`](./features/workbench.md) |
| G218 | The eight palettes past `dark`/`light` and the six accents have not been looked at. `ember-*`, `contrast-*`, `navy-*` and `violet-*` are written against the token roles rather than drawn and reviewed, and an accent is corrected against the ground by one WCAG loop that nothing checks the output of — so a token that reads wrong in one family, or a seed the correction pushes off-hue, shows up on a screen rather than on the style reference page. The sink's style reference draws every token by name and is the sheet for it, the way an icon has review sheets at three sizes | [`features/workbench.md`](./features/workbench.md), [`tech/ui-and-design.md`](./tech/ui-and-design.md) |
| G219 | Every conversation the window holds shares the window's one render entity, so a streamed token marks the whole window dirty. The clock is quietened rather than reshaped: `Message::ConversationUpdate` in `crates/ubiq/src/app/wire.rs` gates the notify on some surface showing that conversation, and coalesces a burst to one frame. The structural answer is an `Entity` per conversation, so a token notifies one transcript — which moves conversation state off `AppState`, the handle every screen reads through, and is a decision with a cost to state rather than a patch. The open question is where the split stops: the transcript alone, or the composer, the footer and the permission list with it. Weighed in [`inbox/render-velocity-proposal.md`](./inbox/render-velocity-proposal.md) | [`features/chat.md`](./features/chat.md), [`tech/ui-and-design.md`](./tech/ui-and-design.md), [`tech/decisions.md`](./tech/decisions.md) |
| G220 | The Git sidebar's five ref lists are the interface's one long list built whole — `crates/ubiq/src/ui/git/refs.rs` draws every branch, tag and stash row — and one `uniform_list` per section would virtualize nothing: a nested list computes its visible range from its own bounds, so inside the sidebar's own scroller it builds every row regardless. One flat list over all five sections needs a uniform row height, and the section heading is the shared `kit::disclosure` at 32px against 21px ref rows. A latent cost rather than a live one, since the grouping is one pass in `crates/ubiq/src/state/git.rs` and costs nothing per frame: what is left is element count on a repository with thousands of refs | [`features/workbench.md`](./features/workbench.md), [`tech/ui-and-design.md`](./tech/ui-and-design.md) |
| G221 | The diff body sizes itself with an assumed monospace advance. `char_advance()` in `crates/ubiq/src/ui/viewer/diff.rs` turns the widest line's character count into pixels at 0.6 × the resolved font size, because `render()` has no `Window` in scope to shape a line and measure it — the same ratio `vendor/gpui-terminal`'s renderer assumes before its first real measurement. A uniform row cannot grow, so that width is what horizontal scrolling reaches, and an approximation reading short clips the longest line. Upgrade: thread a `Window` down and shape it | [`features/workbench.md`](./features/workbench.md) |
| G222 | The diff's flattened rows are memoised where they are read rather than where they live. `flattened()` in `crates/ubiq/src/ui/viewer/diff.rs` holds four entries in a thread-local keyed on the `FileDiff`'s address and its shape, because the `FileDiff` sits in `FileBody::Diff` (`crates/ubiq/src/state/editor.rs`) and on `GitView`, and neither was in scope for that pass. An address is a safe key only while the diff outlives the frame that read it. Upgrade: build the rows beside the `FileDiff` when the reply lands and hand them in, and the cache goes with them | [`features/workbench.md`](./features/workbench.md) |
| G223 | `returning_from_any_non_ide_mode_restores_the_side_panels` in `crates/ubiq/tests/mode_restore.rs` asserts the round trip against a fixture whose three regions are all shut, which `D94` is what makes correct — so the restore it names is satisfied by a mode switch that restores nothing. What the invariant is worth checking against is a region the fixture opened first: reveal one, leave IDE, come back, and assert it is open again | [`features/workbench.md`](./features/workbench.md) |
| G225 | The bus tape has no owning document, and a per-conversation capture points at it. `Tape` in `crates/ubiq-proto/src/bus.rs` is a ring of the last five hundred messages, dumped to `UBIQ_TAPE_DIR` on request in the one-object-per-line shape `dump_line` writes; `D98`'s capture writes the same lines into the same folder from the first frame on, and both are described only where they are used. Neither is bounded or listed: nothing rotates a capture, nothing says which conversations are recording, and the only way to a file is the tooltip on the menu row that opened it. What is owed is one home for the tape, the capture and the folder they share, and a decision on what bounds a capture | [`features/logs.md`](./features/logs.md), [`tech/transport-contract.md`](./tech/transport-contract.md), [`features/workbench.md`](./features/workbench.md) |

## Related docs

- [`tech/decisions.md`](./tech/decisions.md) — the choices that are settled, and what they cost
- [`INDEX.md`](./INDEX.md) — which document owns which fact
- `_meta/feedback.md` — proposals about the documentation rather than the product
