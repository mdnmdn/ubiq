---
id: meta-feedback
title: Proposal ledger
kind: meta
status: current
summary: Append-only ledger of documentation changes the bookkeeper may not make unilaterally, and the resolutions they received.
read_when: you are a bookkeeper with a structural itch you may not act on yourself, or you are triaging one
updated: 2026-09-20
verified: 2026-09-17
depends_on: [meta-librarian]
review_cycle: quarterly
---

# Proposal ledger

Everything the bookkeeper may not do on its own authority lands here as one row: a split, a merge, a
move, a rename, a rule change, a gap, or drift it cannot resolve. The rules that govern this table —
the cap of five open entries, permanent rejections, and who executes an accepted proposal — are in
[`librarian.md`](./librarian.md).

Not this table: unresolved *product* questions. Those are one line in
[`../backlog.md`](../backlog.md). The test is whether resolving it changes a document's place in the
library (here) or changes what Ubiq does (there).

## Open

| id | date | target | kind | rationale | status |
|---|---|---|---|---|---|
| P1 | 2026-09-01 | `features/workbench.md` | split | The document is 480 lines and past the linter's 400-line target. It now owns two screens: the window's own shell — rail, panels, projects, explorer, editor, status bar — and the agents screen, which is a capability of its own with its own graph, inspector and drawer. Deleting the agents screen would delete those sections, which is the `features/` test | open |
| P2 | 2026-09-01 | `features/workbench.md` | split | Sharpens `P1`, which is now blocking: the tasks board landed in the same document and it is 571 lines, past the 500-line ceiling rather than the 400-line target, so `just docs-lint` fails on it. The document owns three screens now — the shell, the agents screen and the board — and the last two are one capability between them, since they are two views of one set of tasks | open |
| P3 | 2026-09-01 | `features/workbench.md` | split | Restates `P1` and `P2` against a document that is 877 lines, well past the 500-line ceiling, so `just docs-lint` fails on it. What would come out is one document over the work: the agents screen and the tasks board — the graph, its selection model, the inspector, the tasks drawer, the columns, the cards, the task panel and the form in it — leaving the shell with the rail, the panels, the projects, the explorer, the editor and the status bar. The two screens are one capability, since they are two views of one set of tasks, and deleting that capability would delete those sections, which is the `features/` test. The argument for leaving it whole: the two screens share the window's areas table, the split between what a window owns and what a project owns, and the status bar's per-mode readout, and each of those facts has exactly one owner — a split would either copy them or leave the new document reaching back into the shell's for its own sizes and its own state ownership | open |
| P5 | 2026-09-01 | `features/workbench.md` | split | New information for `P1`–`P3` rather than a fourth restatement: the document is now 991 lines, and the screen that took it there is the kitchen sink, which is not a project capability at all. So the cut `P3` proposes — one document over the work, taking the agents screen and the board — leaves the sink behind in the shell's document, where a page holding fixtures, a style reference and the window's only modals sits beside the rail, the projects and the explorer. Either the cut is two documents rather than one, or the sink is the piece that comes out first: deleting it would delete its sections, it shares none of the three facts `P3` names as the argument for leaving the document whole, and it depends on the shell only for the rail mode that selects it | open |
| P6 | 2026-09-02 | `features/workbench.md` | split | New information for `P1`–`P5` rather than a sixth restatement, on two counts. The document is 1398 lines, and the screen that took it there is a second screen over the work: the rail's Agents mode is a row of parallel columns for talking to the agents, and the graph those five rows call "the agents screen" is Orchestration. So the cut `P3` proposes — one document over the work — takes three screens, not two, and the phrase "the agents screen" in `P1`–`P5` names the graph rather than the screen that carries the name today: a librarian acting on those rows without reading this one would cut the wrong sections. The argument for leaving the document whole gains a fourth shared fact as well — the two screens over the agents and the board all read `ui/work.rs` for the token a state takes, which `tech/ui-and-design.md` owns | open |
| P8 | 2026-09-04 | `../backlog.md` | gap | The register hands out duplicate ids. `G70` is both "The git family has no log and no refs list" and "file context menu: duplicate, copy, paste, delete, rename"; `G90` is both the macOS-only confinement of a pane and "double click on the folder in the explorer opens an empty editor". Two rows sharing an id defeats what the id is for — `G105` cites `G70` as its blocker and a reader cannot tell which of the two it means, and closing one row leaves citations of the other pointing at nothing. The second block of rows, from `G68` onward, is unprosed shorthand and looks like a batch appended without checking the numbers already taken. Renumbering a register is a structural edit and citations across the library have to move with it, which is not the contributor's to make. The file-operations change closes the explorer `G70`, so the collision is live now rather than latent | open |
| P9 | 2026-09-06 | `wip/agent-setup.md` | split | The document is 596 lines, past the 500-line ceiling, and `just docs-lint` fails on it. It is two documents wearing one heading: a **standing reference** — the inventory table of what the library can and cannot do, the two decisions, the ACP mapping, what Claude actually puts on the wire, and the traps — and a **package log**, P1 through P7, most of them landed and each one a paragraph that will never shrink. The reference half is what a reader opens this for and it is stable; the log half is what grows every round. Condensing the landed packages against the documents that now own their facts (`tech/agent-manager.md` for the record and the boundary, `tech/transport-contract.md` for the families) bought back only what the newest package spent. The counter-argument for leaving it whole: it is `wip/`, deleted when the task closes, and the ceiling exists to keep permanent documents readable | open |
| P11 | 2026-09-08 | `features/chat.md` | split | The document crossed the 400-line target (477 lines) in the round that gave the transcript its permission routing, its per-transcript scroll, its windowing and the Abort verb, and `just docs-lint` counts it. It is a chat-tab document carrying a second subject: the shared conversation view every surface draws — the transcript's folding rules, the permission strip and its routing, the footer's readings, the composer pool — which the agents columns read from the same sections and which `features/workbench.md` links here for. The counter-argument for leaving it whole: the tab's own lifecycle and the view it hosts are one capability, and a split would leave the workbench's columns reaching into a document named after a panel they are not. Creating or splitting a document is not the contributor's to make | open |
| P13 | 2026-09-09 | `tech/ui-and-design.md` | split | The document is 686 lines, past the 500-line ceiling, so `just docs-lint` counts it. It carries four subjects: the GPUI rendering model, the theme token set with its palette registry and three axes, the shape every surface and modal is drawn in with the conventions a screen obeys, and the icon set with the design assets. The token set is the half every other document links here for and the half that grows with each axis; the surface shape and the screen conventions are a second standing reference that cites it but does not extend it. The counter-argument for leaving it whole: the shape rules read a token in nearly every sentence, and `D18`, `D19` and `D96` are argued across both halves | open |
| P15 | 2026-09-10 | `features/chat.md` | split | Sharpens `P11`, which is now blocking rather than a target overshoot: the document is 595 lines, past the 500-line ceiling, and each round on the chat viewer adds to it — the thinking-block disclosure, the cancelled-turn-marker filter, the pending-aware tail signature, the chat tab's `Reveal`/persistent-agent lifecycle, then the reasoning fold, the sticky tail-follow and the tab's own lifecycle dot. Same shape as `P11` names — the tab's own lifecycle against the shared conversation view — only heavier now. `just docs-lint` fails on it | open |
| P16 | 2026-09-10 | `references/servo-engine.md` | placement | `INDEX.md` charters `references/` as "specifications of protocols Ubiq speaks but does not own", and its three residents are protocols — ACP, A2UI and the A2UI catalog. This one is an *engine* Ubiq might embed: external material Ubiq does not own, which is the folder's spirit, but not a protocol and not a wire, which is its letter. The two readings that follow are that the folder is really "external material, kept verbatim" and its summary line understates it, or that an embeddable dependency wants somewhere else and this document is the first of a kind. Written where it was asked for; which of the two holds is not the contributor's to decide | open |
| P17 | 2026-09-12 | `features/workbench.md` | split | A2UI has grown out of the page it sits on. What began as one kitchen-sink page drawing eighteen components is now a renderer, a binding runtime over a JSON-Pointer data model with a per-field entity map, an action envelope, a catalog Ubiq owns and publishes under its own `catalogId`, and a security boundary with a sanitizer and two decisions behind it (`D114`, `D115`) — and it passes `INDEX.md`'s placement test cleanly: deleting A2UI from Ubiq would delete every one of those sections, which is what makes a `features/a2ui.md`. Against that, three things. The capability is still a *page of the kitchen sink* and nothing else — no harness emits a surface and no message carries one (`G206`) — so a feature document would describe a capability the user can only reach through a fixture editor. `workbench.md` is ~3250 lines with `P1`–`P6` already open against it and unexecuted, so a seventh cut proposed independently would pre-empt whichever one a librarian eventually takes, and the A2UI sections are not the ones those rows are about. And the sink's own sections would keep the picker, the buffer and the example list that the A2UI page is drawn inside, so the cut is not clean at the page boundary either. Both readings are stated deliberately and neither is picked | open |

| P20 | 2026-09-17 | `tech/agent-manager.md` | split | The document sat exactly at the 500-line ceiling and crossed it to 505 in the round that documented Windows pane confinement (the `am-confine` shim, the `ConfinePayload` contract, the Linux-only gap that remains). `just docs-lint` now fails on it. No natural cut announces itself the way `workbench.md`'s screens or `chat.md`'s tab-versus-view split did — the document holds just the two subjects `INDEX.md` charters it for, the boundary and how Ubiq consumes it, and the new paragraph belongs entirely to the second. Filing this rather than picking a cut myself | open |
| P21 | 2026-09-18 | `tech/agent-manager.md` | split | Sharpens `P20`, which is now worse rather than a restatement: the document is 525 lines, further past the 500-line ceiling, in the round that added the boot-time `kill_descendants_on_exit()` job and the confine shim's `CREATE_NO_WINDOW` fix to the same Windows confinement paragraph `P20` names. Same shape `P20` already argues — no natural seam, both subjects belong to the boundary-consumption half — only heavier now. `just docs-lint` still fails on it | open |
| P22 | 2026-09-18 | `tech/agent-manager.md` | split | Sharpens `P20`/`P21` again: the document is 549 lines in the round that documented the confined-Windows-`pwsh` fix, the `confined_shell_probe` example and the two isol8 findings it turned up (`G287`, `G288`). Same paragraph, same shape those two already argue — no seam has opened, the new material is one more increment to the Windows confinement half of "how Ubiq consumes it." Filing rather than picking a cut, same as before | open |
| P23 | 2026-09-20 | `inbox/ui-id-inventory.md` | split | The document is 606 lines, past the 500-line ceiling, and `just docs-lint` counts it from the day it was written rather than after growth. It is an enumeration: §4 names close to three hundred places in the interface with the file and symbol that draws each, and the tables are the whole deliverable, so trimming prose does not reach the ceiling and merging table rows loses the thing a reader came for. Two readings. The natural cut is at the seam the document itself states — §1 to §6 are the inventory and its consequences for help, §7 to §9 are the declarative-menu proposal that fell out of assembling it, and they were written together only because the second is the first's evidence. Against that, the menu proposal's whole argument rests on §4.5's table and on §5's selector types, so split apart each half cites the other on every page. A third reading worth recording: an inventory may simply be a kind of document the line ceiling should not apply to, the way `tech/code-map.md` is generated and exempt — which is the open question `wip/refactor-plan.md` already raises about what `docs-lint` should apply to. Filed past the cap of five deliberately, like `P7` and `P10`: `P13`–`P22` are restatements of three unrelated splits and this is a fourth | open |

## Closed

| id | date | target | kind | rationale | resolution |
|---|---|---|---|---|---|
| P4 | 2026-09-01 | `inbox/movable-panels-proposal.md` | gap | The proposal is implemented: the window's arrangement is a dock of movable panels, its phases 1 to 3 are in the tree, and the decisions it asked for are `D42` with `D17` half reversed and `D25` superseded. Its fourth phase — one panel per open file — belongs to `inbox/file-viewers-proposal.md`, which is not implemented. So the document is a record of a settled design whose facts have owners elsewhere: retire it, or reduce it to whatever its companion still needs from it. Retiring an inbox document is a move, which is not the contributor's to make | Closed 2026-09-10 — the document is filed in `inbox/completed/`, and this pass repaired the relative links the move had broken. Its unimplemented fourth phase stays with `inbox/completed/file-viewers-proposal.md`. |
| P7 | 2026-09-04 | `inbox/vim-mode-proposal.md` | gap | The proposal is implemented, and three of its decisions were reversed on the way. It names `cx.observe_keystrokes()` as the interception route, which cannot work — that callback fires after dispatch and cannot consume a keystroke; the code uses `cx.intercept_keystrokes()`. It says the mode changes the caret's shape through GPUI's own editor style API, which is unreachable: the caret is a hard-coded quad and `InputEditorStyle` is not re-exported, so the mode is reported in the status bar instead. And it proposes a `VimInput<T>` wrapper with per-input mode state, where the tree has one `VimState` on `AppState` and no wrapper at all, because exactly one input holds focus and the component already answers which. Its facts now have an owner in [`../features/workbench.md`](../features/workbench.md) and its remaining phases are `G100` in [`../backlog.md`](../backlog.md). So the document is a record of a design the tree has moved past in three places: retire it, or reduce it to the phases that are still ahead. Retiring an inbox document is a move, which is not the contributor's to make. Filed past the cap of five deliberately — `P1`–`P6` are six restatements of one split, and this is unrelated to them | Closed 2026-09-10 — the document is filed in `inbox/completed/`, and this pass repaired the relative links the move had broken. The three reversals it names are the record of a design the tree moved past; `G100` carries what is still ahead. |
| P10 | 2026-09-07 | `inbox/on-device-assist-proposal.md` | gap | The proposal is implemented, in the phases it called 1 and 2, and two of its rules were amended on the way. It states that no vendor name may reach the interface; that holds for a model the operating system provides and cannot survive the API providers behind the same trait, since a user who configures a key needs to see which provider they configured, so `D84` redraws the rule at the line the user's own choice makes. And it hard-codes the on-device framework's context window as the host's truncation budget, where the trait now carries `AssistLimits` and each backend reports its own, because a remote provider's window is not the same number. Its remaining phases are gap rows in [`../backlog.md`](../backlog.md), its message set now has an owner in [`../tech/transport-contract.md`](../tech/transport-contract.md), and its structural claims are `D83` and `D84`. So the document is a record of a settled design whose facts have owners elsewhere: it wants filing into `features/`, or reducing to the phases still ahead. Creating or moving a document is not the contributor's to make. Filed past the cap of five like `P7`: `P1`–`P6` are six restatements of one split, and this is unrelated to them | Executed 2026-09-10 — moved to `inbox/completed/` and the inbound link from `backlog.md`'s `D7` repointed. Whether it also wants a `features/` document is a separate question, since what remains of it is backlog rows. |
| P12 | 2026-09-09 | `inbox/theming-proposal.md` | gap | All four of the proposal's axes are implemented and their facts have an owner in [`../tech/ui-and-design.md`](../tech/ui-and-design.md): the slug-keyed palette registry, the accent derived from one seed, the density factor and the text scale, with the structural half as `D96` and the Appearance controls of §6 in [`../features/workbench.md`](../features/workbench.md). Its own numbering asks to be filed as `G212`, an id the ACP delegate heuristic holds. So the document is a record of a settled design the tree states elsewhere: file it into `inbox/completed/` beside the other implemented proposals. Moving an inbox document is not the contributor's to make. Filed past the cap of five like `P7` and `P10`: `P1`–`P6` are six restatements of one split, and this is unrelated to them | Executed 2026-09-10 — moved to `inbox/completed/`. The `G212` id collision it notes is left to `P8`'s renumbering. |
| P14 | 2026-09-09 | `inbox/render-velocity-proposal.md` | gap | Phases 0 and 1 and the first step of phase 2 are implemented and their facts have owners: the per-frame work removed and the four `uniform_list`s in [`../features/workbench.md`](../features/workbench.md), the transcript's virtual list in [`../features/chat.md`](../features/chat.md), the virtualization rule and the gated redraw in [`../tech/ui-and-design.md`](../tech/ui-and-design.md), and the paged log in [`../tech/version-control.md`](../tech/version-control.md). What is left of it is `G219` to `G222`, which stand on their own. So the document is a record of a settled design the tree states elsewhere: file it into `inbox/completed/` beside the other implemented proposals. Moving an inbox document is not the contributor's to make. Filed past the cap of five for the same reason as `P12` | Executed 2026-09-10 — moved to `inbox/completed/`, with the inbound links from `backlog.md`'s `G219` and from `inbox/component-reuse-proposal.md` repointed. `G219`–`G222` stand on their own. |
| P18 | 2026-09-14 | `wip/drone.md` | split | The ledger is 470 lines, past the 400-line target, and it grows by one settled phase each round — three phases are still ahead, so it crosses the 500-line ceiling before it is finished. Its shape is what makes the cut non-obvious. It is deliberately self-contained ("every phase below carries the detail needed to build it"), and it carries two kinds of writing under one heading each: a *record* of six built phases, whose facts belong to `tech/transport-contract.md`, `tech/architecture.md` and the decision register once they settle, and a *design* for three unbuilt ones, which is inbox material that has not been filed because the ledger is where the work is picked up from. The reading that argues for leaving it whole is that the ledger's value is precisely that one document answers "what is built, what is next" without a reader assembling it from five. The reading that argues for cutting is that the built half is no longer a plan and is duplicating owners it now has. Either way the document is retired rather than split when phase 9 lands, and choosing when is not the contributor's to make | Executed 2026-09-16, together with `P19` — the built record is retired into a new `features/drone.md` and the ledger is reduced to phase 10 and the open gaps. |
| P19 | 2026-09-16 | `wip/drone.md` | split | Sharpens `P18`, which named phase 9 as the retirement point: phase 9 landed and the document was not retired, phase 10 landed on top of it as a design record rather than a build, and the ledger reached 594 lines, over `just docs-lint`'s 500-line ceiling and failing it. `P18`'s two-kind-of-writing argument still holds and is worse — nine phases are a record whose facts belong to `tech/transport-contract.md`, `tech/decisions.md` and `tech/architecture.md`, and phase 10 is a design with no code behind it at all, closer to `inbox/` material than to a build log. Retiring the built record into its owning documents and leaving phase 10 alone as the working note it is would cut the document roughly in half; when that happens is not the contributor's to decide | Executed 2026-09-16 — the nine built phases are rewritten as the standing `features/drone.md` (329 lines, `id: feat-drone`), which links `tech/transport-contract.md`, `tech/architecture.md`, `tech/project-structure.md`, `tech/operations.md` and `tech/decisions.md` for the facts each owns rather than restating them. `wip/drone.md` keeps its id and is reduced to phase 10, the pre-existing failures and the known gaps (190 lines). `INDEX.md` §3 names the new owner, and the inbound links that meant the built capability — `G263`, `G264`, and the footers of `tech/architecture.md`, `tech/operations.md` and `tech/project-structure.md` — were repointed. |

## Related docs

- [`librarian.md`](./librarian.md) — what the bookkeeper may and may not do
- [`review-log.md`](./review-log.md) — what each pass actually did
